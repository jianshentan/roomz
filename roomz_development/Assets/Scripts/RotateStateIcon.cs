using UnityEngine;
using System.Collections;

public class RotateStateIcon : MonoBehaviour {
	public Camera GUI_camera;
	// Use this for initialization
	void Start () {
		GUITexture texture = GetComponent<GUITexture>();
	}
	// Update is called once per frame
	void Update () {
	
	}
}
